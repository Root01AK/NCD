services:
  frontend:
    build:
      context: ./ncd/frontend
      dockerfile: Dockerfile
    image: ncd-frontend-react:latest
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always
    networks:
      - ncd_net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: ncd-backend-django:latest
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL:-mysql://${DB_USER:-ncd_app}:${DB_PASSWORD:-ncd@2@26Yrg}@${DB_HOST:-db}:${DB_PORT:-3306}/${DB_NAME:-default}}
      - DB_HOST=${DB_HOST:-db}
      - DB_PORT=${DB_PORT:-3306}
      - DB_NAME=${DB_NAME:-default}
      - DB_USER=${DB_USER:-ncd_app}
      - DB_PASSWORD=${DB_PASSWORD:-ncd@2@26Yrg}
      - DJANGO_SETTINGS_MODULE=ncd_project.settings
      - DJANGO_DEBUG=${DJANGO_DEBUG:-False}
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY:-django-insecure-ncd-yrgcare-platform-secret-key-2026}
    restart: always
    networks:
      - ncd_net

  db:
    image: mysql:8.0
    restart: always
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD:-rootchangeme}
      - MYSQL_DATABASE=${DB_NAME:-ncd}
      - MYSQL_USER=${DB_USER:-ncd_user}
      - MYSQL_PASSWORD=${DB_PASSWORD:-changeme}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./ncd/DB/ncd.sql:/docker-entrypoint-initdb.d/01-ncd.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD:-rootchangeme}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ncd_net

volumes:
  mysql_data:

networks:
  ncd_net:
    driver: bridge
