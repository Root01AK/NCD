# Copy this to .env and fill in real values before deploying
# Coolify: set these as Environment Variables in the service settings

# Application port on the host
APP_PORT=80

# Database credentials
DB_NAME=ncd
DB_USER=ncd_user
DB_PASSWORD=changeme
DB_ROOT_PASSWORD=rootchangeme
